<Window x:Class="WpfApp_feleves.Views.InputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Adatbekérés" Height="180" Width="350"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="lblQuestion" Grid.Row="0" Text="Kérdés" TextWrapping="Wrap" FontWeight="Bold"/>
        <TextBox x:Name="txtInput" Grid.Row="1" Margin="0,10,0,0"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" IsDefault="True" Click="OK_Click" MinWidth="80" Margin="5" />
            <Button Content="Mégse" IsCancel="True" MinWidth="80" Margin="5" />
        </StackPanel>
    </Grid>
</Window>